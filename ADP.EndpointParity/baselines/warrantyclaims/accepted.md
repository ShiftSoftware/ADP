- [2026-09-02 12:49:03] WarrantyClaims / FullAccess / DealerFinancial.DETAIL.8000001
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:04] WarrantyClaims / FullAccess / DealerFinancial.DETAIL.8000002
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:04] WarrantyClaims / FullAccess / WarrantyCertificate.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:04] WarrantyClaims / FullAccess / WarrantyClaim.DETAIL.8000001
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:05] WarrantyClaims / FullAccess / WarrantyClaim.DETAIL.8000002
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:05] WarrantyClaims / FullAccess / WarrantyClaim.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:05] WarrantyClaims / FullAccess / WarrantyInvoice.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:05] WarrantyClaims / Restricted / DealerFinancial.DETAIL.8000001
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:06] WarrantyClaims / Restricted / DealerFinancial.DETAIL.8000002
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:06] WarrantyClaims / Restricted / WarrantyCertificate.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:06] WarrantyClaims / Restricted / WarrantyClaim.DETAIL.8000001
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:07] WarrantyClaims / Restricted / WarrantyClaim.DETAIL.8000002
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:07] WarrantyClaims / Restricted / WarrantyClaim.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
- [2026-09-02 12:49:07] WarrantyClaims / Restricted / WarrantyInvoice.LIST
  reason: SPIKE-11 convention delta, identical in kind to the eight already accepted for ADP.ClaimableItems and inherited from the framework upgrade - not caused by any code this step wrote. A NULL foreign key now yields a NULL/absent member where the removed .DefaultEntityToDtoAfterMap() produced a non-null placeholder: {"Value":"","Text":null,"Nested":null} for a scalar ShiftEntitySelectDTO (ReferenceWarrantyClaim) and "" for a hash-id string member (ReferenceWarrantyClaimID, CampaignID), because AutoMapper rendered a null source as the empty string on conversion to string. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value, and the generated projection emits `e.X.HasValue ? ... : null`. The parity seed has no reference claim and no campaign on these rows, so every affected case is the null-FK case. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all. NOT affected: the five withheld dealer members, which show ZERO differences - DealerFinancial.LIST matches its baseline exactly, which is control #1 on the exposure hazard.
