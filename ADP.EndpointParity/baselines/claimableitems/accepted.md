- [2026-09-02 02:20:01] ClaimableItems / FullAccess / CampaignVinEntry.DETAIL.7020001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name.
- [2026-09-02 02:20:02] ClaimableItems / FullAccess / ClaimableItem.DETAIL.7010001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name.
- [2026-09-02 02:20:02] ClaimableItems / FullAccess / ItemClaim.DETAIL.7040001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name. ALSO on this case: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. A NULL foreign key now yields a NULL member, where the removed .DefaultEntityToDtoAfterMap() produced the non-null placeholder { "Value": "", "Text": null, "Nested": null }. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value; the old helper set { Value = "" } unconditionally once it found the member blank. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all.
- [2026-09-02 02:20:02] ClaimableItems / FullAccess / ServiceCampaign.DETAIL.7000001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. A NULL foreign key now yields a NULL member, where the removed .DefaultEntityToDtoAfterMap() produced the non-null placeholder { "Value": "", "Text": null, "Nested": null }. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value; the old helper set { Value = "" } unconditionally once it found the member blank. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all.
- [2026-09-02 02:20:03] ClaimableItems / Restricted / CampaignVinEntry.DETAIL.7020001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name.
- [2026-09-02 02:20:03] ClaimableItems / Restricted / ClaimableItem.DETAIL.7010001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name.
- [2026-09-02 02:20:03] ClaimableItems / Restricted / ItemClaim.DETAIL.7040001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. The removed .DefaultEntityToDtoAfterMap() back-filled a scalar ShiftEntitySelectDTO with { Value = <FK> } and ALWAYS left Text null (none of these entities declares [ShiftEntityKeyAndName], so the global ShiftEntityBase->SelectDTO converter produced { Value = "", Text = null } and the AfterMap filled only Value). The replacement convention emits MappingHelpers.ToSelectDTO(entity.<FK>, entity.<Nav>.Name), which also populates Text from the navigation. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Additive: Value is unchanged, Text goes from absent to the navigation name. ALSO on this case: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. A NULL foreign key now yields a NULL member, where the removed .DefaultEntityToDtoAfterMap() produced the non-null placeholder { "Value": "", "Text": null, "Nested": null }. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value; the old helper set { Value = "" } unconditionally once it found the member blank. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all.
- [2026-09-02 02:20:04] ClaimableItems / Restricted / ServiceCampaign.DETAIL.7000001
  reason: SPIKE-11 convention delta, inherited from the framework upgrade and not caused by any code this step wrote. A NULL foreign key now yields a NULL member, where the removed .DefaultEntityToDtoAfterMap() produced the non-null placeholder { "Value": "", "Text": null, "Nested": null }. MappingHelpers.ToSelectDTO(long? id, ...) returns null when the id has no value; the old helper set { Value = "" } unconditionally once it found the member blank. Predicted from the framework source at commit 7830966 BEFORE this run produced it. Verification Rule 5 keeps null, empty and absent distinct, which is why this shows as a difference at all.
