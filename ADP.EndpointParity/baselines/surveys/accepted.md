- [2026-09-02 01:06:27] Surveys / FullAccess / Survey.LIST.afterRemove
  reason: Harness fix, not a product change. The CRUD template omitted $top on the afterRemove LIST variant, which the framework requires. Verified inert: the git diff of the re-captured golden is exactly one line - the request URL - with response status and body byte-identical to the pre-migration recording. The migration itself produced zero response differences on this case.
- [2026-09-02 01:06:27] Surveys / FullAccess / BankQuestion.LIST.afterRemove
  reason: Harness fix, not a product change. The CRUD template omitted $top on the afterRemove LIST variant, which the framework requires. Verified inert: the git diff of the re-captured golden is exactly one line - the request URL - with response status and body byte-identical to the pre-migration recording. The migration itself produced zero response differences on this case.
- [2026-09-02 01:06:28] Surveys / FullAccess / ScreenTemplate.LIST.afterRemove
  reason: Harness fix, not a product change. The CRUD template omitted $top on the afterRemove LIST variant, which the framework requires. Verified inert: the git diff of the re-captured golden is exactly one line - the request URL - with response status and body byte-identical to the pre-migration recording. The migration itself produced zero response differences on this case.
- [2026-09-02 01:06:35] Surveys / Restricted / Survey.LIST.afterRemove
  reason: Same harness $top fix as the FullAccess accept. Under this read-only grant the missing page size made the request 400 ("please specify a page size ... unrestricted data sets"), so the golden recorded an error envelope and asserted NOTHING about the list projection; it now returns 200 with rows. STATED LIMIT: the response half therefore has no pre-migration counterpart and this case is NOT a pre/post control - it becomes one from this baseline forward. The list projection under this grant is already controlled by the plain LIST case, which carried $top all along and verifies unchanged.
- [2026-09-02 01:06:36] Surveys / Restricted / BankQuestion.LIST.afterRemove
  reason: Same harness $top fix as the FullAccess accept. Under this read-only grant the missing page size made the request 400 ("please specify a page size ... unrestricted data sets"), so the golden recorded an error envelope and asserted NOTHING about the list projection; it now returns 200 with rows. STATED LIMIT: the response half therefore has no pre-migration counterpart and this case is NOT a pre/post control - it becomes one from this baseline forward. The list projection under this grant is already controlled by the plain LIST case, which carried $top all along and verifies unchanged.
- [2026-09-02 01:06:36] Surveys / Restricted / ScreenTemplate.LIST.afterRemove
  reason: Same harness $top fix as the FullAccess accept. Under this read-only grant the missing page size made the request 400 ("please specify a page size ... unrestricted data sets"), so the golden recorded an error envelope and asserted NOTHING about the list projection; it now returns 200 with rows. STATED LIMIT: the response half therefore has no pre-migration counterpart and this case is NOT a pre/post control - it becomes one from this baseline forward. The list projection under this grant is already controlled by the plain LIST case, which carried $top all along and verifies unchanged.
