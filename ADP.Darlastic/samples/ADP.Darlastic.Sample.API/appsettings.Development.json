{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "ConnectionStrings": {
    "Registry": "Server=localhost\\SQLEXPRESS;Database=Darlastic-TIQ;Integrated Security=true;TrustServerCertificate=true"
  },
  "Sample": {
    "//": "Authenticates every caller as a developer. Development-only; the host refuses to start if this is set anywhere else.",
    "AllowDevAuth": true
  },
  "Kestrel": {
    "Endpoints": {
      "Http": { "Url": "http://localhost:5080" }
    }
  }
}
