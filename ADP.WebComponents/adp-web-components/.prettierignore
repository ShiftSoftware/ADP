node_modules
dist
build
www
components.d.ts
src/components.d.ts
src/locale-mapper.ts
locale-mapper.ts

src/global/types/generated

# Generated by `npm run build:templates` — compiled Tailwind output and vendored Alpine
src/templates/harness.css
src/templates/vendor

# Frozen prototypes — they record what a design looked like when a decision was
# taken, so reformatting them churns the record for nothing. tiq-test-drive-slots
# lives in that folder but is a live harness page, not a record, so it formats.
src/templates/prototypes
!src/templates/prototypes/tiq-test-drive-slots.html
