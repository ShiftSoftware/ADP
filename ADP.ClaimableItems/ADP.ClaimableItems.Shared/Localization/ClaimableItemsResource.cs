namespace ShiftSoftware.ADP.ClaimableItems.Shared.Localization;

// Marker type for the module's own localization resource (ClaimableItemsResource.resx / .ru.resx).
// Resolved through the consumer's AddLocalization() via IStringLocalizer<ClaimableItemsResource> — the
// same pattern the framework uses for ShiftBlazor (its ShiftBlazorLocalizer wraps a framework-owned
// Resource). The module owns its translations so every consumer gets them without re-authoring.
public class ClaimableItemsResource
{
}
